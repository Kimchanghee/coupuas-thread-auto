{
  "name": "coupuas-thread-auto-site",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test tests_js/*.test.mjs"
  },
  "dependencies": {
    "@vercel/queue": "0.5.0"
  }
}
